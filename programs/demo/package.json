{
  "name": "demo",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "cargo build-bpf",
    "deploy": "solana program deploy ../../target/deploy/demo.so",
    "logs": "solana logs $(cat program_id.md)",
    "proto": "pb-rs instructions.proto state.proto -d src --dont_use_cow"
  }
}
